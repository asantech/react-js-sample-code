import CustomSpinner from "@components/common/spinners/CustomSpinner"

function Loading() {
  return (
    <div>
      <CustomSpinner />
      Loading ...
    </div>
  )
}

export default Loading
